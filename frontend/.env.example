# Leave unset for local dev — vite.config.js proxies /api to the backend.
# Set this when the frontend and backend are deployed as separate hosts/domains
# (e.g. frontend on Vercel/Netlify, backend on Vercel Functions):
# VITE_API_BASE_URL=https://your-backend.vercel.app/api
